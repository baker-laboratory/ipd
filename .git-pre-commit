#!/bin/bash

if ruff check ipd --fix; then
    echo ruff ok;
else
    echo ruff failed; exit 1;
fi
if PYTHONPATH=. python -m ipd.tools.yapf_fast ipd; then
    echo files all formatted;
else
    echo some files changed, retry commit; exit 1;
fi

